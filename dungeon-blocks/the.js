/*
Until this is working properly, it will be kept in a comment.
because it currently breaks the.css.


const dblocks = {};
dblocks.create = function(content){
	document.write(`<div class="block-stack">${content}</div>`);
}

*/
